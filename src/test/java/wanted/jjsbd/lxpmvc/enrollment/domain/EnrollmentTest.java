package wanted.jjsbd.lxpmvc.enrollment.domain;

class EnrollmentTest {
/*
	@Test
	@DisplayName("정상적인 더하기 연산이 성공해야 한다")
	void add_Success() {
		// given (준비)
		Enrollment calculator = new Enrollment();

		// when (실행)
		int result = calculator.add(2, 3);

		// then (검증)
		Assertions.assertEquals(5, result);
	}
*/
	/*
	@Test
	@DisplayName("CI 빌드 실패 테스트를 위해 일부러 실패하는 케이스")
	void add_FailedForCI() {
		// given (준비)
		Enrollment calculator = new Enrollment();

		// when (실행)
		int result = calculator.add(2, 3);

		// then (검증 - 2+3은 5이지만 일부러 99를 넣어 실패를 유도합니다)
		Assertions.assertEquals(99, result);
	}
	 */
}
